# Copy this to .env.local and fill in your values

# Database connection
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=migration_db

# Environment name (dev/ci/prod)
ENV_NAME=dev

# Optional: GitHub metadata (auto-set in CI)
# GITHUB_ACTOR=your_username
# GITHUB_SHA=commit_hash